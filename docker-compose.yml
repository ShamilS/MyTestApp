services:
  api:
    image: ghcr.io/shamils/mytestapp:latest
    container_name: my-test-app-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped